# Plugin lockfile — NOT tracked (2026-08: stop tracking lazy-lock.json).
# lazy-lock.json is generated locally by :Lazy update/sync and stays per
# machine; nvim-pack-lock.json (vim.pack era) is retired.
lazy-lock.json
